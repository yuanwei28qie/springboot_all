package com.study.cn.springbootall.rabbitmq;

/**
 * @author huwei
 * @date 2019/9/4 16:39
 * <p> description:rabbitMQ 基于AMQP协议（高级消息队列协议），是erlang语言开发，
 * 开源的消息代理和队列服务器
 * 作用：通过普通协议在不同的应用服务器中共享数据；跨语言和跨平台使用
 * 优点：
 *      开源。
 *      服务器与服务器之间的链接消耗性能低。
 *      集群模式丰富。
 *      集成springAMQP。
 *
 *AMQP协议模型；
 *      producor（生产者）-> server(服务端)-> virtual host（虚拟主机）
 *      ->exchange（交换机）->queue（队列）->customer(消费者)
 *
 *
 * </>
 */
public class DemoRabbitMQ {
}
